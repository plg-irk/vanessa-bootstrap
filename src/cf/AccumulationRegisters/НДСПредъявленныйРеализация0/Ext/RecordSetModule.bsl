#Если Сервер ИЛИ ТолстыйКлиентОбычноеПриложение ИЛИ ВнешнееСоединение Тогда
	
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Регистратор = Отбор.Регистратор.Значение;

	Если ТипЗнч(Регистратор) = Тип("ДокументСсылка.Операция") Тогда 
		Возврат;
	КонецЕсли;
	
//	НеИспользоватьПоставщикаВРегистреНДСПредъявленныйРеализация0 = ОбщегоНазначения.ПрочитатьДанныеИзХранилища(
//		ЭтотОбъект[0].Организация, "НеПрименяетсяПоставщикДляРегистраНДСПредъявленныйРеализация0");
    НеИспользоватьПоставщикаВРегистреНДСПредъявленныйРеализация0 = Ложь;
     
	Если НеИспользоватьПоставщикаВРегистреНДСПредъявленныйРеализация0 <> Неопределено 
			И НеИспользоватьПоставщикаВРегистреНДСПредъявленныйРеализация0 Тогда
		НеЗаполненныйПоставщик = Справочники.Контрагенты.ПустаяСсылка();
		Для каждого Проводка Из ЭтотОбъект Цикл
			Проводка.Поставщик = НеЗаполненныйПоставщик;
		КонецЦикла;
	КонецЕсли;

КонецПроцедуры

#КонецЕсли