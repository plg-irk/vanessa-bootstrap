
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Не ПустаяСтрока(Параметры.АдресХранилищаСотрудники) Тогда
		ТаблицаСотрудников.Загрузить(ПолучитьИзВременногоХранилища(Параметры.АдресХранилищаСотрудники));
		ВыборСотрудникаИзТаблицы = Параметры.ПодборПродавцов;
	КонецЕсли;
	
	ПодборПродавцов = Параметры.ПодборПродавцов;
	НастроитьКассировДляВхода = Параметры.НастроитьКассировДляВхода;
	РежимВыбораПродавца = Параметры.РежимВыбораПродавца;
	Если Параметры.Свойство("РежимКассыСамообслуживания") Тогда
		РежимКассыСамообслуживания = Параметры.РежимКассыСамообслуживания;
		Если Параметры.Свойство("ПользовательРМК") И ЗначениеЗаполнено(Параметры.ПользовательРМК) Тогда
			ПользовательРМК = Параметры.ПользовательРМК;
		Иначе
			ПользовательРМК = Пользователи.ТекущийПользователь();
		КонецЕсли;
	КонецЕсли;
	ОтборПоказатьВсех = РежимКассыСамообслуживания Или НастроитьКассировДляВхода;
	ОбработатьОтборСотрудников();
	
	Если РежимВыбораПродавца Тогда
		Заголовок = НСтр("ru = 'Продавцы'");
	Иначе
		Заголовок = НСтр("ru = 'Кассиры'");
	КонецЕсли;
	ОбщегоНазначенияРМКПереопределяемый.ПриСозданииНаСервере(ЭтотОбъект, Отказ, СтандартнаяОбработка);
	
	Элементы.Очистить.Видимость = Параметры.ПродавецВыбран;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ОтборСотрудниковПриИзменении(Элемент)
	ОбработатьОтборСотрудников();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыТаблицаЧеков

&НаКлиенте
Процедура СписокПродавцовВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ЗафиксироватьВыбор();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Выбрать(Команда)
	
	ЗафиксироватьВыбор();
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура Очистить(Команда)
	
	Закрыть(Истина);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ОбработатьОтборСотрудников()
	
	ОтборСотрудников = ?(ПоказатьНеТолькоСотрудников, 0, 1);
	ОтборСотрудниковСервер();
	
КонецПроцедуры

&НаКлиенте
Процедура ЗафиксироватьВыбор()
	Если ВыборСотрудникаИзТаблицы Тогда
		ТекущиеДанные = Элементы.ТаблицаСотрудников.ТекущиеДанные;
	Иначе
		ТекущиеДанные = Элементы.СписокПродавцов.ТекущиеДанные;
	КонецЕсли;
	
	Если ТекущиеДанные = Неопределено Тогда
		Закрыть();
	Иначе
		Закрыть(?(ВыборСотрудникаИзТаблицы, ТекущиеДанные.ИдентификаторСотрудника, ТекущиеДанные.Продавец));
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОтборСотрудниковСервер()
	ОбщегоНазначенияРМКПереопределяемый.ПереопределитьЗапросДоступныхПродавцов(ЭтотОбъект);
КонецПроцедуры

&НаКлиенте
Процедура ТаблицаСотрудниковВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	ЗафиксироватьВыбор();
КонецПроцедуры

&НаКлиенте
Процедура ОтборПоказатьВсехПриИзменении(Элемент)
	УстановитьОтборПоказатьВсехНаСервере();
КонецПроцедуры

&НаСервере
Процедура УстановитьОтборПоказатьВсехНаСервере()
	ОбщегоНазначенияРМКПереопределяемый.ПереопределитьЗапросДоступныхПродавцов(ЭтотОбъект);
КонецПроцедуры

#КонецОбласти
