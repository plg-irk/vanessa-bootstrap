#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Начальное заполнение и обновление информационной базы.
Процедура НачальноеЗаполнение() Экспорт

	Если ОбщегоНазначения.ЭтоПодчиненныйУзелРИБ() Тогда
		Возврат;
	КонецЕсли;
	
	Заполнен = Обновить(ПолучитьМакет("ПредопределенныеЗначения").ПолучитьТекст(), Ложь);
	Если НЕ Заполнен И ОбщегоНазначения.ПодсистемаСуществует("ЗарплатаКадрыПодсистемы.УчетСтраховыхВзносов") Тогда
		КлассификаторПредопределенные
			= РегистрыСведений["ТарифыСтраховыхВзносов"].ПолучитьМакет("ПредопределенныеЗначения"); 
		Обновить(КлассификаторПредопределенные.ПолучитьТекст(), Ложь);
	КонецЕсли;
КонецПроцедуры

// Обновляет данные регистра.
// Параметры:
//   ТекстXML - Строка
//   ПолучатьДанныеИзСервиса - Булево
//
// Возвращаемое значение:
//   - Булево - Истина, если классификатор успешно обновлен
//
Функция Обновить(ТекстXML, ПолучатьДанныеИзСервиса = Истина) Экспорт
	Возврат РегламентированнаяОтчетностьУСН.ОбновитьКлассификатор(ТекстXML, ПолучатьДанныеИзСервиса);
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает размеры тарифов взносов в разрезе лет и видов тарифа, 
// установленные Федеральным законом.
//
// Параметры: 
// 	нет
//
// Возвращаемое значение:
//	Таблица значений, соответствующая по структуре регистру сведений ТарифыСтраховыхВзносовУНФ.
//
Функция ТаблицаТарифовСтраховыхВзносов() Экспорт
	ТекстXML = ПолучитьМакет("ПредопределенныеЗначения").ПолучитьТекст();
	СведенияОКлассификаторе = ОбщегоНазначения.ЗначениеИзСтрокиXML(ТекстXML);
	Возврат РегламентированнаяОтчетностьУСН.ПолныеИменаВСсылки(СведенияОКлассификаторе.Данные, СоздатьНаборЗаписей().ВыгрузитьКолонки());
КонецФункции

#КонецОбласти

#КонецЕсли