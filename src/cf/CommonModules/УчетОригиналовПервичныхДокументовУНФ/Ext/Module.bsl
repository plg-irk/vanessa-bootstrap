#Область ПрограммныйИнтерфейс

// Возвращает состояние оригинала первичного документа.
//
// Параметры:
//  ДокументСсылка - ДокументСсылка - Ссылка на документ, для которого необходимо получить состояние оригинала.
//
// Возвращаемое значение:
//  СправочникСсылка.СостоянияОригиналовПервичныхДокументов - Состояние оригинала первичного документа.
//  Если сведения о состоянии оригинала не удалось получить или они не содержат свойства
//  "СостояниеОригиналаПервичногоДокумента", то возвращается пустая ссылка
//  справочника СостоянияОригиналовПервичныхДокументов.
Функция СостояниеОригиналаПервичногоДокумента(ДокументСсылка) Экспорт

	ТекущееСостояниеОригинала = УчетОригиналовПервичныхДокументов.СведенияОСостоянииОригиналаПоСсылке(ДокументСсылка);

	Если ТипЗнч(ТекущееСостояниеОригинала) <> Тип("Структура") Тогда
		Возврат Справочники.СостоянияОригиналовПервичныхДокументов.ПустаяСсылка();
	КонецЕсли;

	Если Не ТекущееСостояниеОригинала.Свойство("СостояниеОригиналаПервичногоДокумента") Тогда
		Возврат Справочники.СостоянияОригиналовПервичныхДокументов.ПустаяСсылка();
	КонецЕсли;

	Возврат ТекущееСостояниеОригинала.СостояниеОригиналаПервичногоДокумента;

КонецФункции

#Область УстаревшиеПроцедурыИФункции

// Устарела. См. УчетОригиналовПервичныхДокументов.ПриСозданииНаСервере_ФормаДокумента
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма документа.
Процедура ПриСозданииНаСервере_ФормаДокумента(Форма) Экспорт
	УчетОригиналовПервичныхДокументов.ПриСозданииНаСервере_ФормаДокумента(Форма);
КонецПроцедуры

#КонецОбласти

#КонецОбласти