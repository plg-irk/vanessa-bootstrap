#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ПодарочныйСертификат") Тогда
		ПодарочныйСертификат = Параметры.ПодарочныйСертификат;
	КонецЕсли;
	
	Если Параметры.Свойство("НомерСертификата") Тогда
		НомерСертификата = Параметры.НомерСертификата;
	КонецЕсли;
	
	Если Параметры.Свойство("Документ") Тогда
		Если ТипЗнч(Параметры.Документ) = Тип("ДанныеФормыСтруктура") Тогда
			Документ = Параметры.Документ.Ссылка;
		Иначе
			Документ = Параметры.Документ;
		КонецЕсли;
	КонецЕсли;
	
	Если Параметры.Свойство("КОплате") Тогда
		КОплате = Параметры.КОплате;
	КонецЕсли;
	
	Если Параметры.Свойство("Сумма") Тогда
		Сумма = Параметры.Сумма;
	КонецЕсли;
	
	Если Параметры.Свойство("ВозвратОплаты") Тогда
		ВозвратОплаты = Параметры.ВозвратОплаты
	КонецЕсли;
	
	Если Параметры.Свойство("Организация") Тогда
		Организация = Параметры.Организация
	КонецЕсли;
	
	Товары = Неопределено;
	ТоварыКЗагрузке = ТоварыКОплате.Выгрузить();
	Если Параметры.Свойство("Товары", Товары) Тогда 
		Если ТипЗнч(Товары) = Тип("ТаблицаЗначений") Тогда
			ТоварыКЗагрузке = Товары;
		ИначеЕсли ТипЗнч(Товары) = Тип("ДанныеФормыКоллекция") Тогда
			ТоварыКЗагрузке = Товары.Выгрузить(,"Номенклатура, Характеристика, Всего"); 
			ТоварыКЗагрузке.Колонки["Всего"].Имя = "Сумма";			
		ИначеЕсли ТипЗнч(Товары) = Тип("Массив") Тогда
			Для Каждого стрТовары Из Товары Цикл
				НовСтрока = ТоварыКЗагрузке.Добавить();
				ЗаполнитьЗначенияСвойств(НовСтрока, стрТовары);
				НовСтрока.Сумма = стрТовары.Всего;
			КонецЦикла;
		КонецЕсли;	
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.АктВыполненныхРабот") Тогда
		ТоварыКЗагрузке = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Документ, "РаботыИУслуги").Выгрузить().Скопировать(,"Номенклатура, Характеристика, Всего");
		ТоварыКЗагрузке.Колонки["Всего"].Имя = "Сумма";
	ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.ЗаказПокупателя") Тогда
		ДанныеДокумента = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Документ, "ВидОперации, Запасы, Работы");
		ТоварыКЗагрузке = ДанныеДокумента.Запасы.Выгрузить().Скопировать(,"Номенклатура, Характеристика, Всего");
		ТоварыКЗагрузке.Колонки["Всего"].Имя = "Сумма";
		Если ДанныеДокумента.ВидОперации = Перечисления.ВидыОперацийЗаказПокупателя.ЗаказНаряд Тогда
			Товары = ДанныеДокумента.Работы.Выгрузить().Скопировать(,"Номенклатура, Характеристика, Всего");
			Товары.Колонки["Всего"].Имя = "Сумма";
			Для Каждого стрРаботы Из Товары Цикл
				ЗаполнитьЗначенияСвойств(ТоварыКЗагрузке.Добавить(), стрРаботы);
			КонецЦикла;
		КонецЕсли;
	ИначеЕсли ЗначениеЗаполнено(Документ) Тогда
		ТоварыКЗагрузке = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Документ, "Запасы").Выгрузить().Скопировать(,"Номенклатура, Характеристика, Всего");
		ТоварыКЗагрузке.Колонки["Всего"].Имя = "Сумма";
	КонецЕсли;
	ТоварыКОплате.Загрузить(ТоварыКЗагрузке);

	ОбновитьДанныеСертификата();
	УправлениеДоступностьюИВидимостью();
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если Источник = "ПодключаемоеОборудование"
		И ВводДоступен() Тогда
		Если ИмяСобытия = "ScanData" Тогда
			Если Параметр[1] = Неопределено Тогда
				Результат = Параметр[0]; // Достаем штрихкод из основных данных
			Иначе
				Результат = Параметр[1][1]; // Достаем штрихкод из дополнительных данных
			КонецЕсли;
			
			ПоискПоНомеруЗавершение(Результат, Новый Структура("ТекНомер", Результат));
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВидСертификатаПриИзменении(Элемент)
	
	ОбновитьДанныеСертификата();
	УправлениеДоступностьюИВидимостью();
	
КонецПроцедуры

&НаКлиенте
Процедура НомерСертификатаПриИзменении(Элемент)
	
	ОбновитьДанныеСертификата();
	УправлениеДоступностьюИВидимостью();
	
КонецПроцедуры

&НаКлиенте
Процедура НомерСертификатаНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("ПодборДляОплаты", Не ВозвратОплаты);
	СтруктураПараметров.Вставить("Отбор", Новый Структура("Владелец", ПодарочныйСертификат));
	ОткрытьФорму("Справочник.СерииНоменклатуры.ФормаВыбора", СтруктураПараметров, Элемент);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Если Элементы.НомерСертификата.Доступность И НомерСертификата.Пустая() Тогда
		ТекстСообщения = НСтр("ru = 'Не указан номер сертификата!'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения,, "НомерСертификата", "НомерСертфиката");
		Возврат;
	КонецЕсли;
	
	Если Сумма > КОплате Тогда
		ТекстСообщения = НСтр("ru = 'Сумма оплаты не может превышать сумму документа!'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения,, "Сумма", "Сумма");
		Возврат;
	КонецЕсли;
	
	Если ИспользоватьСерииНоменклатуры Тогда
		
		Если Не ВозвратОплаты Тогда
			Если Сумма > Остаток Тогда
				ТекстСообщения = НСтр("ru = 'Превышен остаток средств по сертификату!'");
				ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения,, "Сумма", "Сумма");
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		
		Если Сумма > Номинал Тогда
			ТекстСообщения = НСтр("ru = 'Превышен номинал сертификата!'");
			ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения,, "Сумма", "Сумма");
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЧастичноеПогашение Тогда
		СуммаПогашенияСертификата = Сумма;
	ИначеЕсли ИспользоватьСерииНоменклатуры Тогда
		СуммаПогашенияСертификата = Остаток;
	Иначе
		СуммаПогашенияСертификата = Номинал;
	КонецЕсли;
	
	Если Сумма = 0 Тогда
		ПараметрыЗакрытия = Неопределено;
	Иначе
		ПараметрыЗакрытия = Новый Структура;
		ПараметрыЗакрытия.Вставить("ПодарочныйСертификат", ПодарочныйСертификат);
		ПараметрыЗакрытия.Вставить("НомерСертификата", НомерСертификата);
		ПараметрыЗакрытия.Вставить("Сумма", Сумма);
		ПараметрыЗакрытия.Вставить("СуммаПогашенияСертификата", СуммаПогашенияСертификата);
		ПараметрыЗакрытия.Вставить("ВидОплаты", ПредопределенноеЗначение(
			"Перечисление.ВидыБезналичныхОплат.ПодарочныйСертификат"));
	КонецЕсли;
		
	Закрыть(ПараметрыЗакрытия);
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть(Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоискПоНомеру(Команда)
	
	ТекНомер = "";
	ПоказатьВводЗначения(Новый ОписаниеОповещения("ПоискПоНомеруЗавершение", ЭтотОбъект, Новый Структура("ТекНомер", ТекНомер)), ТекНомер, НСтр("ru = 'Введите номер'"));
	
КонецПроцедуры

&НаКлиенте
Процедура ПоискПоНомеруЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ТекНомер = ?(Результат = Неопределено, ДополнительныеПараметры.ТекНомер, Результат);
	
	НуженВыборИзСписка = Ложь;
	
	Если Не ПустаяСтрока(ТекНомер) Тогда
		НуженВыборИзСписка = ВыполнитьПоискСертификатаПоНомеру(ТекНомер);
	КонецЕсли;
	
	Если НуженВыборИзСписка Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения("ДобавитьВыбранныйСертификат", ЭтаФорма);
		СтруктураПараметров = Новый Структура("Адрес", АдресВХранилище);
		ОткрытьФорму("Справочник.Номенклатура.Форма.ФормаВыбораСертификата", СтруктураПараметров,,,,, ОписаниеОповещения);
	Иначе
		УправлениеДоступностьюИВидимостью();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ДобавитьВыбранныйСертификат(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(ЭтаФорма, Результат);
	СтруктураРеквизитовСертификата = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ПодарочныйСертификат, "ИспользоватьСерииНоменклатуры, Номинал");
	ЗаполнитьЗначенияСвойств(ЭтаФорма, СтруктураРеквизитовСертификата);
	Сумма = Мин(КОплате, Результат.Остаток);
	
	УправлениеДоступностьюИВидимостью();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УправлениеДоступностьюИВидимостью()
	
	Элементы.НомерСертификата.Доступность = ПодарочныйСертификат.ИспользоватьСерииНоменклатуры;
	
	Если ВозвратОплаты Тогда
		Элементы.КОплате.Заголовок = НСтр("ru = 'К возврату'");
	КонецЕсли;
	
	Элементы.Сумма.СписокВыбора.Очистить();
	
	ЗаголовокСуммы = ?(ВозвратОплаты, НСтр("ru = 'к возврату'"), НСтр("ru = 'доступно к оплате'"));
	ПредставлениеКОплате = СтрШаблон("%1 (%2)", ДоступноКОплате, ЗаголовокСуммы);
	Элементы.Сумма.СписокВыбора.Добавить(ДоступноКОплате, ПредставлениеКОплате);
	
	Если ПодарочныйСертификат.ИспользоватьСерииНоменклатуры Тогда
		Если ЗначениеЗаполнено(Остаток) И Не ВозвратОплаты Тогда
			ПредставлениеОстаток = СтрШаблон(НСтр("ru = '%1 (остаток)'"), Остаток);
			Элементы.Сумма.СписокВыбора.Добавить(Остаток, ПредставлениеОстаток);
		КонецЕсли;
	Иначе
		ПредставлениеНоминал = СтрШаблон(НСтр("ru = '%1 (номинал)'"), ПодарочныйСертификат.Номинал);
		Элементы.Сумма.СписокВыбора.Добавить(ПодарочныйСертификат.Номинал, ПредставлениеНоминал);
	КонецЕсли;
	
	Сумма = Мин(ДоступноКОплате, Остаток);
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьДанныеСертификата()
	
	ТаблицаОплатыСертификатами = РаботаСПодарочнымиСертификатами.ПустаяТаблицаОплатПодарочнымиСертификатами();
	ТекущаяОплата = ТаблицаОплатыСертификатами.Добавить();
	ТекущаяОплата.ПодарочныйСертификат = ПодарочныйСертификат;
	ТекущаяОплата.НомерСертификата = НомерСертификата;
	ТекущаяОплата.Регистратор = Документ;
	
	ТаблицаТоварыУслуги = РаботаСПодарочнымиСертификатами.ПустаяТаблицаТоваровПодарочныхСертификатов();
	Для Каждого СтрокаИсходнойТаблицы Из ТоварыКОплате Цикл
		ЗаполнитьЗначенияСвойств(ТаблицаТоварыУслуги.Добавить(), СтрокаИсходнойТаблицы);
	КонецЦикла;
	
	ОрганизацияПродажи = ?(ЗначениеЗаполнено(Организация), Организация, Неопределено );
	Если НЕ ЗначениеЗаполнено(ОрганизацияПродажи)
		И ЗначениеЗаполнено(Документ)
		И ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Документ, "Организация") Тогда
		ОрганизацияДокумента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Документ, "Организация");
		ОрганизацияПродажи = Константы.УчетПоКомпании.Компания(ОрганизацияДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(ОрганизацияПродажи) Тогда
		ТаблицаТоварыУслуги.ЗаполнитьЗначения(ОрганизацияПродажи, "Организация");
	КонецЕсли;

	РезультатыПроверки = РаботаСПодарочнымиСертификатами.ДопустимыеСуммыОплатыСертификатами(ТаблицаОплатыСертификатами, 
		ОрганизацияПродажи, ТаблицаТоварыУслуги);
	ДоступноКОплате = 0;
	ДоступноПосертификату = 0;
	Остаток = 0;
	Номинал = 0;
	ДатаОкончания = Дата('00010101');
	Для Каждого ОписаниеСертификата Из РезультатыПроверки Цикл
		ИспользоватьСерииНоменклатуры = ОписаниеСертификата.ИспользоватьСерииНоменклатуры;
		ЧастичноеПогашение = ОписаниеСертификата.ЧастичноеПогашение;
		Если ИспользоватьСерииНоменклатуры И ОписаниеСертификата.ПроизвольныйНоминал Тогда
			Номинал = ОписаниеСертификата.Остаток;
		Иначе
			Номинал = ОписаниеСертификата.Номинал;
		КонецЕсли;
		МожноОплатитьДаннымСертификатом = Мин(Номинал, ОписаниеСертификата.ДоступнаяСумма);
		ДоступноПосертификату = ДоступноПосертификату + МожноОплатитьДаннымСертификатом;
		Остаток = Остаток + ОписаниеСертификата.Остаток;
		Если ОписаниеСертификата.ДатаОкончанияДействия = Дата('00010101') 
			ИЛИ ДатаОкончания > ОписаниеСертификата.ДатаОкончанияДействия Тогда
			ДатаОкончания = ОписаниеСертификата.ДатаОкончанияДействия;
		КонецЕсли;
	КонецЦикла;
	ДоступноКОплате = Мин(ДоступноПосертификату, КОплате);
		
КонецПроцедуры

&НаСервере
Функция ВыполнитьПоискСертификатаПоНомеру(Номер)
	
	Результат = РаботаСПодарочнымиСертификатами.ВыполнитьПоискСертификатаПоНомеру(Номер);
	
	Если Результат.Количество() = 0 Тогда
		ТекстСообщения = СтрШаблон(НСтр("ru = 'Не найден сертификат по номеру: %1'"), Номер);
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Результат.Количество() = 1 Тогда
		ЗаполнитьЗначенияСвойств(ЭтаФорма, Результат[0]);
		СтруктураРеквизитовСертификата = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ПодарочныйСертификат,
			"ИспользоватьСерииНоменклатуры, Номинал");
		ЗаполнитьЗначенияСвойств(ЭтаФорма, СтруктураРеквизитовСертификата);
		Сумма = Мин(КОплате, Результат[0].Остаток);
		Возврат Ложь;
	Иначе
		АдресВХранилище = ПоместитьВоВременноеХранилище(Результат);
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

#КонецОбласти